# homelab-docs-public
How to setup a homelab based on Unraid and Docker containers

## Getting started
- Install python
- Clone repo & navigate to it
- Create virtual environment `python -m venv venv`
- Install: `pip install mkdocs-material`
- Install: `pip install mkdocs-nav-weight`

## Run locally
`mkdocs serve`